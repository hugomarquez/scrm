%li
  - if activity.owner
    %div
      %h4= activity.owner.person.full_name
      %span added a new Account
      - if activity.trackable
        = link_to activity.trackable.name, activity.trackable
      - else
        which has since been created
      = time_ago_in_words(activity.created_at)
      %span ago
